# Toolkit for annotation platform
This repo provides tools for converting annotation of the platform to popular formats

# Usage
Replace `ANNO_DIR` and `DATA_DIR` in `constants.py` which correspond to annotation folder and output folder, respectively.

Run following command
```
python convert2coco.py
```
